import { Typing } from "./components/typing/typing";

export default function App() {
  return (
    <main className="w-full min-h-screen flex items-center justify-center font-mono">
      <Typing />
    </main>
  );
}
